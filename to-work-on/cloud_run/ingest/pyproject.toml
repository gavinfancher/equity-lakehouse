[project]
name = "finpipe-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "boto3>=1.34",
  "google-cloud-storage>=2.18",
]

# Note: pendulum is NOT needed in the worker — only get_keys.py / control.py
# (which run from the repo root, not in the container) use it.
