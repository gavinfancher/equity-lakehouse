[project]
name = "equity-lakehouse"
version = "0.1.0"
description = "Sample Iceberg writes against the Lakehouse REST catalog"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["google-auth", "pandas", "pyarrow", "pyiceberg[pyarrow]"]

[tool.uv]
package = false
